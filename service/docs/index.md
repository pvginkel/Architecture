# Service docs

Topic docs for the `service/` subproject — small, single-topic design docs that a plan's
required-reading list is built from, per the [documentation model](../../docs/documentation-model.md).
Cross-cutting and system-level design lives in the [root docs](../../docs/index.md).

## Topic docs

_None yet — adoption baseline (2026-06-26). Seed this scope with `/update-docs` (it builds a scope
from nothing and reconciles drift), or add a topic doc and list it here as conventions get written
down._

## Maintaining this index

A pure fan-out: one entry per topic doc — a path plus one precise line on what's inside. No entry
without a doc; no doc missing from the index. Plan-writers survey this directory to assemble a
reading list.
