# Code-health grader exclusions. Gitignore syntax; loaded alongside .gitignore.
# Application code that IS graded: tooling/ (Python pipeline), viewer/src, service/src.

# Orchestration & helper scripts (not application code)
scripts/
tools/

# Data, schema, and pipeline config — not code
schema/
views/
arch/
pipeline-producers.yaml
pipeline-producers.schema.yaml
views.schema.yaml

# Docs (incl. the self-producer architecture artifact under docs/architecture/)
docs/

# Generated (vocab is emitted by tooling/generate.py)
viewer/src/generated/

# Per-file exclusions can also be added inline with `# health: ignore`.
