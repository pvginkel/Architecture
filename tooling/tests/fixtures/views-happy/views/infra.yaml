id: infra
label: Infrastructure
description: Compute nodes, plus the Keycloak instance without its neighbours.
icon: Network
predicate:
  kinds: [Node]
includeUnexpanded:
  - "/^ss:keycloak-prd,/"
neighbourDepth: 0
