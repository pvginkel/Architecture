# Authoritative ordering of the curated views. The viewer renders one tab per
# id in this order, opens the first (landscape), and appends the synthesised
# `everything` view last. Adding a view = a file under views/ + an entry here.
order:
  - landscape
  - design-assistant
  - intercom
  - scanner
  - delivery
  - infrastructure
  - application
  - identity
  - data
  - home-automation
  - iot
